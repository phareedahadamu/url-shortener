[build]
  # Generate Prisma client + build Next.js
  command = "npx prisma generate && next build"
  publish = ".next"        # App Router output directory
  environment = { 
    NODE_ENV = "production"
  }

[[plugins]]
  package = "@netlify/plugin-nextjs"